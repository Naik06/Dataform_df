config { type: "table" }
SELECT
ArticleID,
date(CommentDateUpdated) CommentDate,
COUNT(DISTINCT CommentID) AS NumComments
FROM ${ref("coral_comments_fct")}
WHERE 1=1
AND dw_partition_date between  "2022-04-01" and "2022-04-30"
AND CommentStatus = 'ACCEPTED'
GROUP BY 1,2